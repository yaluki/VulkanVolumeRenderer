\contentsline {lstlisting}{\numberline {1}To find the siblings of a given voxel inside the input list we use this static algorithm. Aside from the index of the first voxel we also need the side length of the cubic volume, labeled as $N$ in the above listing.}{2}
\contentsline {lstlisting}{\numberline {2}Finding the next block start index needs to cover three cases.}{2}
